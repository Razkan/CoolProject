namespace Interfaces.Model.Enum
{
    public enum SpellType
    {
        Missing = -1,

        Attack,
        Effect,
        Spiritual,
        Automatic,
        Detection,
        Defense
    }
}